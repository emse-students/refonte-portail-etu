// See https://svelte.dev/docs/kit/types#app.d.ts
// The showcase is fully public and read-only: no session, no user locals.

declare global {
	namespace App {
		// interface Error {}
		// interface Locals {}
		// interface PageData {}
		// interface PageState {}
		// interface Platform {}
	}
}

export {};
