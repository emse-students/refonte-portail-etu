// Configuration globale pour toutes les routes API
// Désactiver le prerendering et forcer le SSR pour toutes les routes sous /api
export const prerender = false;
export const ssr = true;
export const csr = false;
