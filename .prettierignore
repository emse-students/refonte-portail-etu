# Build outputs
build/
.svelte-kit/
dist/
node_modules/

# Generated (inlang / paraglide).
# `project.inlang/cache/` is REGENERATED by every `paraglide-js compile`, which `prepare` runs on
# every install - so CI produced it before reaching `format:check` and the gate failed on a file
# nobody wrote. git already ignores it (project.inlang/.gitignore); Prettier does not read
# .gitignore, so it has to be named here too.
project.inlang/.meta.json
project.inlang/README.md
project.inlang/cache/
src/lib/paraglide/

# Static files
static/
src/lib/assets/

# Package files. This project installs with bun and nothing else; the npm and pnpm lockfiles it
# used to name have not existed here for a long time, and `bun.lockb` is the old BINARY format that
# `bun.lock` replaced.
bun.lock

# Environment
.env
.env.*

# Misc
*.log
.DS_Store
