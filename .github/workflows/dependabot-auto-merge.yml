name: Dependabot auto-merge

# Squash-merges Dependabot pull requests once the full check suite on the PR head commit is green,
# and only for updates whose failure mode this repository can actually SEE.
#
# The decision itself lives in `.github/scripts/dependabot-auto-merge.sh`, one implementation for
# both entry points below. The header there explains the ceiling; this file explains WHEN it runs.
#
# ---------------------------------------------------------------------------------------------
# WHY THERE ARE THREE TRIGGERS, AND WHY THE CONVERGENT ONE IS NOT THE CLOCK
# ---------------------------------------------------------------------------------------------
# `workflow_run` is the fast path: a PR-gating workflow finishes, and the pull request it belongs to
# is evaluated seconds later. It is also, on its own, NOT ENOUGH - and until 2026-08-31 it was the
# only trigger this workflow had. A pull request whose checks completed days ago will never receive
# another `workflow_run`, so an event-only automation acts on what it happened to catch and on
# nothing else; it cannot recover from being disabled, from a runner outage, or from being written
# after the queue it is meant to drain. Canari measured exactly that: seven mergeable green pull
# requests sat untouched with the workflow enabled and working.
#
# THE SWEEP IS THE CONVERGENT PATH: it enumerates every open Dependabot pull request and applies
# the same decision to each, so the correct state is reached from ANY starting state. Termination
# comes from durable state on GitHub - a pull request is merged, or it carries a comment naming the
# test that would let it merge - never from how often the sweep runs.
#
# WHAT THE SWEEP HANGS OFF IS A PUSH TO `main` RATHER THAN AN HOUR. The hourly `schedule:` below
# is kept as a floor and no more; it DOES fire, with hours of jitter, and the correction to what
# was written here on 2026-08-31 is on that block. A push to `main` is the event this repository
# sees often and reliably, so answering it is the closest the queue gets to converging on the
# thing that disturbed it.
#
# ---------------------------------------------------------------------------------------------
# WHY MERGING SEVERAL IN ONE SWEEP IS SAFE
# ---------------------------------------------------------------------------------------------
# Two pull requests green in isolation are not evidence that their COMBINATION is green - nothing
# tests it before the merges. What makes that acceptable is the `verify` job added to `deploy.yml`
# on 2026-08-31: the dispatch this workflow issues now runs `test.yml` on the merged tree, and the
# deploy job refuses to run unless it passed. So a combination that only breaks together reddens the
# dispatch and stops there. `main` can go red; the server cannot follow it.
#
# The ceiling in `.github/scripts/dependabot-auto-merge.sh` is currently EMPTY, and that is a
# measured answer rather than an omission - both candidates were closed by writing the gate. Read
# the `case` block for which, and for what would earn a new entry.

on:
  workflow_run:
    # THE PUSH-TRIGGERED WORKFLOW IN THIS LIST IS THE CONVERGENT TRIGGER; the rest are the fast
    # path. Its completion is the closest thing this repository has to "somebody did something",
    # and this workflow answers it with a FULL SWEEP rather than with one pull request. See the
    # schedule below for why that stopped being the schedule's job.
    workflows: ["Run Tests", "Code Analysis"]
    types: [completed]
  schedule:
    # Minute 41 rather than 0: the hour boundary is the busiest slot on shared runners, and this is
    # offset from the sibling repositories' sweeps so they do not queue behind each other.
    #
    # KEPT, AND THE CLAIM WRITTEN HERE ON 2026-08-31 WAS WRONG - it said this cron had produced
    # ZERO runs, measured in the three hours after it landed on `main`. Counted on 2026-09-01,
    # every one of the four repositories had delivered a scheduled sweep: Canari twice, and this
    # one, Sky, MiGallery and Portail-etu once each. A THREE-HOUR WINDOW IS NOT ENOUGH TO CALL A
    # TRIGGER DEAD, and the correction matters more than the demotion it barely changes - a
    # mechanism built on the first quiet interval anybody happened to look at is built on nothing.
    #
    # What DOES hold is the SHAPE of the delivery, measured over seven consecutive days rather
    # than one afternoon: Canari's `code-analysis.yml` asks for `0 2 * * *` and ran at 03:01,
    # 03:09, 08:05, 08:24, 08:47, 12:37 and 14:10 UTC. GitHub does not queue the slots an hourly
    # cron misses, it DROPS them. So this clock arrives, eventually, with hours of jitter - a
    # floor under the worst case and never the thing a verdict waits on.
    - cron: "41 * * * *"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  # `gh workflow run deploy.yml` after a merge. Without it the dispatch 403s and the deploy is
  # skipped exactly as silently as the push that never triggered one.
  actions: write

# One sweep at a time. Two overlapping passes would each read a pre-merge mergeability and race on
# the same branches; `cancel-in-progress: false` because a pass that is already merging must finish.
concurrency:
  group: dependabot-auto-merge
  cancel-in-progress: false

jobs:
  auto-merge:
    name: Merge what this repository has a gate for
    runs-on: ubuntu-latest
    # The scheduled and manual paths carry no event to inspect; the author check that actually
    # protects the merge is inside the script, which runs for every pull request either way. This
    # condition only keeps the `workflow_run` path from waking on unrelated CI, and keeps a fork
    #
    # TWO SHAPES OF `workflow_run` ARE ACCEPTED, and they mean different things. A run triggered
    # by DEPENDABOT'S OWN pull request is the fast path: evaluate that one pull request, seconds
    # after its checks finish. A run triggered by a PUSH TO MAIN is the convergent path: sweep the
    # whole open queue, because a push is the one event this repository sees often and reliably.
    # from sweeping its own queue with this repository's rules.
    if: >-
      github.repository == 'emse-students/refonte-portail-etu' &&
      (github.event_name != 'workflow_run' ||
      (github.event.workflow_run.event == 'pull_request' &&
      github.event.workflow_run.actor.login == 'dependabot[bot]') ||
      (github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_branch == 'main'))
    steps:
      # PINNED TO `main`, NOT TO THE EVENT: on the `workflow_run` path the pull request under
      # evaluation is Dependabot's, and this job holds write permissions. The decision script must
      # come from the trunk, never from the branch it is judging.
      - name: Check out the decision script from main
        uses: actions/checkout@v7
        with:
          ref: main
          sparse-checkout: .github/scripts

      # THE PREDICATE IS TESTED IN THE RUN THAT USES IT. `lib/gate-moves.sh` decides whether a
      # green-but-older check suite is still evidence, and the half that matters - a truncated
      # compare, an unreadable payload - never occurs on a good day, so nothing but a test will
      # exercise it. Bash only, no dependencies, and a failure here stops the sweep BEFORE it
      # merges anything on a predicate that no longer holds.
      - name: Verify the staleness predicate before trusting it
        run: bash .github/scripts/tests/gate-moves.test.sh

      - name: Collect the pull requests to evaluate
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          set -euo pipefail
          list="$RUNNER_TEMP/prs.txt"
          : > "$list"

          # ONLY a Dependabot pull request's own run narrows this to one pull request. Every
          # other entry point - a push to main, the schedule, a manual dispatch - takes the sweep,
          # because none of them is ABOUT a particular branch.
          entry="${{ github.event_name }}/${{ github.event.workflow_run.event }}"
          if [ "$entry" = "workflow_run/pull_request" ]; then
            # Exactly the pull request whose checks just finished.
            gh api "repos/$REPO/commits/$HEAD_SHA/pulls" \
              --jq '.[] | select(.state=="open") | .number' | head -n1 >> "$list"
          else
            # The sweep. `--state open` and the author filter are what make this convergent rather
            # than a re-run of whatever the last event happened to be.
            gh pr list --repo "$REPO" --author app/dependabot --state open \
              --limit 100 --json number --jq '.[].number' >> "$list"
          fi

          echo "PR_LIST=$list" >> "$GITHUB_ENV"
          echo "$(wc -l < "$list") pull request(s) to evaluate:"
          cat "$list"

      - name: Apply the ceiling to each
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          merged=0
          while IFS= read -r pr; do
            [ -z "$pr" ] && continue
            echo "::group::PR #$pr"
            # The script never fails a pull request it merely declines, so a single unmergeable
            # branch cannot stop the sweep.
            if .github/scripts/dependabot-auto-merge.sh "$pr" | tee "$RUNNER_TEMP/out-$pr.txt"; then :; fi
            if grep -q "^MERGED $pr\$" "$RUNNER_TEMP/out-$pr.txt"; then
              merged=$((merged + 1))
            fi
            if grep -q "^STALE $pr\$" "$RUNNER_TEMP/out-$pr.txt"; then
              echo "$pr" >> "$RUNNER_TEMP/stale.txt"
            fi
            echo "::endgroup::"
          done < "$PR_LIST"

          echo "merged=$merged" >> "$GITHUB_ENV"
          echo "merged $merged pull request(s) this pass"

      # WHAT A STALE PULL REQUEST NEEDS, AND WHY THIS STEP CANNOT PROVIDE IT.
      #
      # The script marks a pull request STALE only when the definitions that BUILD a check suite -
      # `.github/workflows/`, `.github/scripts/` - moved between its base and `main`. Then its green
      # checks describe gates that no longer exist, and the branch has to be rebuilt before any
      # verdict on it means anything.
      #
      # NEITHER WAY OF REBUILDING IT IS AVAILABLE TO THIS WORKFLOW, and both were tried:
      #
      #   `PUT /pulls/{n}/update-branch` pushes a merge commit authored by `github-actions[bot]`.
      #   The `pull_request` run it re-triggers is created as `action_required`, parked until a human
      #   clicks Approve; Dependabot then refuses the branch for good ("this PR has been edited by
      #   someone other than Dependabot"); and the entry condition here admits only a `workflow_run`
      #   whose actor is `dependabot[bot]`, so even an approval would not let it back in. The
      #   refresh made the branch unmergeable BY EVERY PATH.
      #
      #   `@dependabot recreate` is refused when the caller is `github-actions[bot]`. Measured on
      #   emse-students/canari#303 on 2026-08-31, three seconds after the ask: "Sorry, only users
      #   with push access can use that command." The default token is not an account, and
      #   Dependabot authorises its commands by push access.
      #
      # So this step REPORTS rather than pretending, and the report is the point: a stale pull
      # request that says nothing is found by hand, a week late. It names what has to happen, ONCE
      # per pull request - a comment re-posted every sweep is noise.
      #
      # Closing that gap is a credential decision rather than a code one: a fine-grained PAT or a
      # GitHub App token with push access would let the ask above succeed. Closing the pull request
      # is NOT the alternative - Dependabot does not recreate a version whose pull request was
      # closed unmerged.
      - name: Report the branches whose gates moved under them
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          MARKER: "<!-- auto-merge-gates-moved -->"
        run: |
          set -uo pipefail
          list="$RUNNER_TEMP/stale.txt"
          if [ ! -s "$list" ]; then echo "no branch is waiting on a rebuild."; exit 0; fi

          body="$RUNNER_TEMP/gates-moved.md"
          cat > "$body" <<EOF
          $MARKER
          **Not auto-merged: the gates moved under this branch.**

          Its checks were produced by workflow definitions \`main\` no longer carries, so they are
          not evidence about what \`main\` gates on now. The branch has to be rebuilt before any
          verdict on it means anything, and this workflow cannot rebuild it: \`update-branch\` leaves
          a head Dependabot refuses, and \`@dependabot recreate\` is only honoured for an account
          with push access.

          **Comment \`@dependabot recreate\` here** and the rest is unattended again.
          EOF
          sed -i 's/^          //' "$body"

          echo "$(wc -l < "$list") branch(es) waiting on a rebuild this workflow cannot perform."
          while IFS= read -r pr; do
            [ -z "$pr" ] && continue
            if gh api "repos/$REPO/issues/$pr/comments" --paginate --jq '.[].body' | grep -qF "$MARKER"; then
              echo "  #$pr: already reported"
            elif gh pr comment "$pr" --repo "$REPO" --body-file "$body"; then
              echo "  #$pr: reported"
            else
              echo "  #$pr: could not post the report - it stays silent, which is the defect"
            fi
          done < "$list"

      # A squash merge made with GITHUB_TOKEN produces a push that triggers NOTHING - github's
      # anti-recursion rule - so `Run Tests` never ran on those merges, `deploy.yml` never saw the
      # `workflow_run` it waits for, and main drifted from the server silently. `workflow_dispatch`
      # is the documented exception to that rule, so this is the one call that closes the gap. ONE
      # dispatch for the whole pass, not one per merge: the deploy takes `main` as it now stands.
      #
      # `skip_ci` is deliberately NOT passed. The dispatch path runs `test.yml` on the merged tree,
      # which is the only thing that ever tests a sweep's updates TOGETHER.
      - name: Dispatch one deploy if anything merged
        # Both halves are load-bearing. The step above runs without `set -e` so one unmergeable
        # branch cannot stop a sweep, which means it can also end early and leave `merged` never
        # written - and an UNSET value is not equal to '0', so testing only that would dispatch a
        # deploy on the one path where nothing is known to have merged.
        if: env.merged != '' && env.merged != '0'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          echo "Dispatching the deploy for $merged merge(s)"
          gh workflow run deploy.yml --repo "$REPO" --ref main
