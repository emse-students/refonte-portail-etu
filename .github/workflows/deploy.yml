name: Deploy via SSH

on:
  workflow_run:
    workflows: ["Run Tests"]
    branches: [main]
    types: 
      - completed

jobs:
  deploy:
    name: Deploy
    runs-on: self-hosted
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build application
        run: bun run build

      - name: Paste files to right location
        run: |
          cd ~/portail-etu
          rm -rf -v !(.env)
          cp ~/actions-runner/_work/refonte-portail-etu/refonte-portail-etu/build/* . -r

      - name: Restart server
        run: |
          pm2 restart portail-etu || pm2 start --interpreter ~/.bun/bin/bun ./index.js --name portail-etu --update-env

