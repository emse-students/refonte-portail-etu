# =================================================================================================
# DEPLOY THE PORTAL - A LIBRARY, CALLED ONLY BY `release.yml`
#
# NO TRIGGERS OF ITS OWN, AND NO ROW OF ITS OWN IN THE ACTIONS LIST, since 2026-09-04. It fired on
# `workflow_run` after `Run Tests` finished on `main`, which made every merge a deployment:
# production was whatever the last green pull request happened to be, and nobody chose it.
# **Nothing deploys on a push any more** - `release.yml` is the only caller, in every repository of
# the ecosystem.
#
# ITS `verify` JOB IS GONE WITH THE DISPATCH PATH THAT NEEDED IT. That job re-ran the whole suite,
# because a dispatch could otherwise deploy a combination nothing had tested together. The release
# preflight READS the verdict that already exists on the commit instead - cheaper, and stricter,
# because it also refuses an ABSENT check, which a re-run cannot even notice.
#
# AND `skip_ci` IS GONE WITH IT. It was the escape hatch for the 2026-08-06 Actions outage that
# dropped the push triggers; the release path has no push trigger to lose, and a flag that skips the
# only evidence a deploy has is a fallback path - reaching one means the primary path failed, and
# the fix belongs there.
#
# IT IS HANDED A COMMIT AND A VERSION rather than working them out. The caller has already put them
# through the gates; recomputing them here would be a second opinion about what is being shipped.
# =================================================================================================
name: Deploy

on:
  workflow_call:
    inputs:
      sha:
        description: "The commit to build and ship. Resolved and gated by the caller."
        required: true
        type: string
      version:
        description: "The version being released, without a leading `v`. Recorded in the run log."
        required: true
        type: string

permissions:
  contents: read

jobs:
  # NO `if:` AND NO `needs:`, and that is the point of being a library. Every condition this job
  # used to carry was about which trigger had fired and whether some other run had gone green; a
  # called workflow runs because its caller decided it should, and the caller's gates are the
  # condition.
  deploy:
    name: Deploy
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          # The commit the caller gated, never `github.sha` - which on a `release` event is the tag
          # and on a dispatch is whatever the branch happens to be by then.
          ref: ${{ inputs.sha }}

      # One bun, named in one place. `.bun-version` is the pin; every workflow
      # that uses setup-bun reads the same file, and pm2 launches the very binary
      # written here, so install, build and runtime can no longer disagree.
      #
      # Two properties of this host force both halves of that pin:
      #
      #  - It is a KVM guest whose CPU advertises no AVX2, so it needs the
      #    `baseline` artifact. setup-bun always fetches the ordinary linux-x64
      #    build, which is why the version is installed by hand here instead.
      #  - Its runtime cannot start on bun 1.3.9 or later. `bun --version`
      #    answers instantly and `bun install` succeeds, but a `bun -e` that only
      #    writes one marker file spins at 100% CPU forever and leaves no marker.
      #    That is what took production down: the server burned CPU inside its
      #    module load, before its first log line and before it bound a port,
      #    while every check that looked at bun still passed. A CD run bisected
      #    it on this machine - 1.3.14 through 1.3.9 all hang, 1.3.8 reaches user
      #    code - so 1.3.8 is the newest release this box can run, and the pin
      #    holds until upstream fixes it or the runtime moves into a container.
      - name: Install the pinned bun this CPU can run
        run: |
          BUN_VERSION="$(tr -d '[:space:]' < .bun-version)"
          curl -fsSL -o /tmp/bun-baseline.zip "https://github.com/oven-sh/bun/releases/download/bun-v$BUN_VERSION/bun-linux-x64-baseline.zip"
          rm -rf /tmp/bun-baseline && mkdir -p /tmp/bun-baseline
          unzip -qo /tmp/bun-baseline.zip -d /tmp/bun-baseline
          mkdir -p "$HOME/.bun/bin"
          install -m 755 /tmp/bun-baseline/bun-linux-x64-baseline/bun "$HOME/.bun/bin/bun"
          echo "$HOME/.bun/bin" >> "$GITHUB_PATH"

          # A version string only proves the binary loads. This box's whole
          # outage was a bun that answered --version and then never reached user
          # code, so the install is not finished until it has run some.
          rm -f /tmp/bun-alive
          timeout 15 "$HOME/.bun/bin/bun" -e 'require("fs").writeFileSync("/tmp/bun-alive","ok")'
          if [ ! -f /tmp/bun-alive ]; then
            echo "::error::bun $BUN_VERSION installs but cannot reach user code on this host"
            exit 1
          fi
          echo "bun $("$HOME/.bun/bin/bun" --version) (baseline), runtime verified / avx2: $(grep -qm1 avx2 /proc/cpuinfo && echo yes || echo no)"

      - name: Install dependencies
        run: |
          rm -rf node_modules
          bun install --frozen-lockfile

      - name: Generate SvelteKit files
        run: bunx svelte-kit sync

      - name: Build application
        run: bun run build

      - name: Paste files to right location
        run: |
          cd ~/portail-etu
          # Wipe everything, including any stale hand-written .env: runtime
          # config is rewritten from GitHub Secrets in the next step, so the
          # server keeps no manual state and a fresh machine deploys identically.
          find . -mindepth 1 -maxdepth 1 -exec rm -rf {} +
          cp ~/actions-runner/_work/refonte-portail-etu/refonte-portail-etu/* . -r

      - name: Write runtime environment from GitHub Secrets
        # Secrets are passed via env (masked in logs) rather than interpolated
        # into the script body. Bun auto-loads ~/portail-etu/.env at runtime, so
        # the pm2-launched server reads these without a rebuild. Empty values are
        # safe: the app code falls back to public defaults for the URLs and
        # disables the avatar proxy when GALLERY_API_KEY is unset.
        env:
          PUBLIC_CANARI_URL: ${{ secrets.PUBLIC_CANARI_URL }}
          GALLERY_API_URL: ${{ secrets.GALLERY_API_URL }}
          GALLERY_API_KEY: ${{ secrets.GALLERY_API_KEY }}
          PORTAL_URL: ${{ secrets.PORTAL_URL }}
        run: |
          cd ~/portail-etu
          cat > .env <<EOF
          PUBLIC_CANARI_URL=${PUBLIC_CANARI_URL}
          GALLERY_API_URL=${GALLERY_API_URL}
          GALLERY_API_KEY=${GALLERY_API_KEY}
          PORTAL_URL=${PORTAL_URL}
          EOF

      - name: Restart server
        run: |
          cd ~/portail-etu
          pm2 restart ./ecosystem.config.cjs --update-env || pm2 start ./ecosystem.config.cjs

      # A deploy that only starts the process proves nothing. On 2026-08-26 every
      # step here reported success while the site served 502: pm2 prints `online`
      # the instant it spawns, so a process that crashes a second later still
      # leaves a green deploy behind, and the restart counter is the only thing on
      # that table that gives it away.
      #
      # This step is the report the deploy never had. It asks the app the one
      # question the whole deploy exists to answer - does it serve? - and when it
      # does not, it prints the crash that nothing else on this box can show,
      # because the runner is the only way in.
      - name: Verify the app actually answers
        run: |
          for attempt in $(seq 1 10); do
            code=$(curl -s -o /dev/null -w '%{http_code}' --max-time 5 http://127.0.0.1:3000/ || true)
            if [ "$code" = "200" ]; then
              echo "The app answered 200 on attempt ${attempt}."
              exit 0
            fi
            echo "Attempt ${attempt}: HTTP ${code:-none}, retrying in 3s..."
            sleep 3
          done

          echo "::error::The app never answered 200. Last status: ${code:-none}."
          # pm2 reports `online` the instant it spawns a process, so its table is
          # not evidence that anything serves; the restart counter and the app's
          # own output are. There is no SSH to this box - printing them here is
          # the only way anyone sees them.
          pm2 describe portail-etu 2>&1 |
            grep -aiE 'status|restarts|uptime|script|exec cwd' || true
          pm2 logs portail-etu --nostream --lines 30 2>&1 |
            grep -av -iE '(secret|token|password|api[_-]?key|authorization|bearer)' || true
          exit 1
